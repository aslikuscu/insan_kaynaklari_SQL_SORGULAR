--sirketin planlama departmanina yeni bir sef atandi maasini belirlemek icin planlama departmaninin ortalama maas, max maas, min maas mikatarlarini getir(not: isten cikanlarda eklenmeli)

select pos.POSITION,
max(p.SALARY) as max_salary,
MIN(p.SALARY) as min_salary,
ROUND(AVG(p.SALARY),2) as avg_salary
from HR.dbo.POSITION as pos
inner join HR.dbo.PERSONN as p on p.POSITIONID=pos.ID
where pos.POSITION = 'PLANLAMA SEFI'
group by pos.POSITION 

select*from HR.dbo.POSITION
select*from HR.dbo.PERSONN